[pytest]
testpaths = tests
# Silenciar deprecations de librerías de terceros (no son de nuestro código):
# - python-jose usa datetime.utcnow() internamente
# - reportlab usa ast.NameConstant internamente
filterwarnings =
    ignore::DeprecationWarning:jose.*
    ignore::DeprecationWarning:reportlab.*
